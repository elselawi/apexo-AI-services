// an array of servers white-listed to access this API
// for now we will leave it empty to allow all servers to access it
// but in the future we can add some servers to this list to restrict access to only those servers

export const whitelist: string[] = [];